{
  "name": "chrismorris.org",
  "description": "Scitechivity! Science. Technology. Creativity. (My Jekyll-powered blog.)",
  "author": "Chris Morris <chris@chrismorris.org> (http://chrismorris.org/)",
  "version": "1.5.0",
  "homepage": "http://chrismorris.org",
  "private": true,
  "main": "chrismorris.org.js",
  "dependencies": {
    "body-parser": "^1.14.2",
    "express": "^4.13.3",
    "mysql": ">= 2.0.0-alpha5"
  },
  "devDependencies": {
    "standard": "^5.4.1",
    "stylus": "^0.53.0"
  },
  "scripts": {
    "build": "standard && npm run build-blog && npm run build-css",
    "build-blog": "jekyll build",
    "build-css": "stylus css/*.styl -o css/ -c",
    "test": "standard",
    "push": "rm -rf _site && npm run build && rsync -a -v -e ssh --delete _site nginx.conf _server node_modules chris@xenon:/home/chris/www/chrismorris.org/",
    "start": "npm run build && jekyll serve --port 3000",
    "watch": "npm run watch-blog",
    "watch-blog": "jekyll serve --watch --port 3000",
    "watch-css": "stylus css/*.styl -o css/ -w"
  },
  "standard": {
    "globals": [ "$", "lightbox" ]
  },
  "bugs": {
    "url": "https://github.com/ChrisMorrisOrg/ChrisMorris.org/issues"
  },
  "engine": "node 5.2.0"
}
